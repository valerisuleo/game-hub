import styles from './expense-tracker.module.scss';

const ExpenseTracker = () => {
    return (
        <div className={styles['container']}>
            <h1>Welcome to ExpenseTracker!</h1>
        </div>
    );
};

export default ExpenseTracker;
