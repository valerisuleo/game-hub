import styles from './home.module.scss';

const Home = () => {
    return (
        <div className={styles['container']}>
            <h1>Welcome to Home!</h1>
        </div>
    );
};

export default Home;
